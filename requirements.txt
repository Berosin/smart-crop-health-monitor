# Web UI
streamlit>=1.32.0

# Deep Learning (disease detection model)
# TensorFlow >=2.15 bundles Keras internally (tf.keras) — no separate
# `keras` package needed, and pinning one risks pulling in Keras 3.x,
# which is not compatible with the tf.keras-style API used here.
tensorflow>=2.15.0

# Classical ML (supplementary scoring / recommendations)
scikit-learn>=1.4.0
joblib>=1.3.0    # model persistence for src/environment_model.py (also a scikit-learn dependency)

# Image processing
opencv-python-headless>=4.9.0

# Data processing
pandas>=2.2.0
numpy>=1.26.0

# Database (built-in, but listed for clarity)
# sqlite3  — part of the Python standard library

# Visualization
plotly>=5.20.0
matplotlib>=3.8.0
seaborn>=0.13.0
tensorboard>=2.15.0

# Utilities
Pillow>=10.2.0

# Live weather integration (src/weather.py — OpenWeatherMap client)
requests>=2.31.0

# PDF report export (src/report_generator.py)
reportlab>=4.1.0

# Icons (Tabler Icons rendered as images for the UI)
pytablericons>=1.0.1